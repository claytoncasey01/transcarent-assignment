package handler

import "github.com/labstack/echo"

func (h *Handler) Register(v1 *echo.Group) {
	// user := v1.Group("/user")
	// user.GET(":id", )

	// posts := v1.Group("/posts")
	// posts.Get(":userId", )
}
